# Django and Core
Django==5.1.4
python-dotenv==1.0.0

# Wagtail CMS (compatible with Django 5.1.4)
wagtail==6.3

# Note: wagtail-localize doesn't support Django 5.1 yet
# We'll add it later when it's updated, or use an alternative
# For now, we can manage translations without it

# Database
# For PostgreSQL (production)
# psycopg2-binary==2.9.10

# Image Processing
Pillow==10.4.0

# QR Code Generation
qrcode[pil]==8.0
segno==1.6.1

# Forms
django-crispy-forms==2.3
crispy-bootstrap5==2024.10

# Utilities
python-slugify==8.0.4

# For production (optional for now)
# gunicorn==22.0.0
# whitenoise==6.7.0