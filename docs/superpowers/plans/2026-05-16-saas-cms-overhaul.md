# VCard SaaS & CMS Overhaul — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrate to uv/pyproject.toml, wire up Wagtail as a full marketing CMS, rebuild the homepage with a clean B-style design, build blog/about/pricing/legal pages, and set up a `dev` branch with GitHub Actions auto-deploy to `devvcard.hosammo.com`.

**Architecture:** Wagtail owns all public marketing URLs (`/`, `/blog/`, `/about/`, `/pricing/`, `/privacy/`, `/terms/`) via a new `cms` Django app. The existing `cards` SaaS app is untouched. Wagtail's catch-all URL is registered last in `urls.py` so Django routes always take precedence.

**Tech Stack:** Django 5.2 LTS, Wagtail 6.4, uv + pyproject.toml, GitHub Actions (appleboy/ssh-action@v1), Passenger WSGI on shared hosting.

---

## File Map

### New files
| File | Purpose |
|------|---------|
| `pyproject.toml` | Single source of truth for all dependencies (replaces requirements.txt) |
| `.python-version` | Pins Python version for uv |
| `cms/__init__.py` | Makes cms a Django app |
| `cms/apps.py` | App config |
| `cms/blocks.py` | All Wagtail StreamField block definitions |
| `cms/models.py` | All Wagtail Page subclasses (HomePage, Blog*, About, Pricing, Legal) |
| `cms/migrations/0001_initial.py` | Auto-generated by makemigrations |
| `cms/migrations/0002_initial_pages.py` | Data migration: seeds page tree |
| `cms/templates/cms/base.html` | Shared marketing nav + footer (B-style) |
| `cms/templates/cms/home_page.html` | Full 9-section homepage |
| `cms/templates/cms/blog_index_page.html` | Blog listing |
| `cms/templates/cms/blog_post_page.html` | Individual blog post |
| `cms/templates/cms/about_page.html` | About page |
| `cms/templates/cms/pricing_page.html` | Full pricing + FAQ |
| `cms/templates/cms/legal_page.html` | Privacy/Terms |
| `vcard_project/settings_dev.py` | Dev environment settings |
| `passenger_wsgi_dev.py` | Passenger entry point for devvcard.hosammo.com |
| `.github/workflows/deploy-dev.yml` | GitHub Actions auto-deploy on push to dev |
| `tests/test_cms_pages.py` | Page model + URL tests |

### Modified files
| File | What changes |
|------|-------------|
| `requirements.txt` | Deleted |
| `requirements-local.txt` | Deleted |
| `vcard_project/settings.py` | Add `cms` to INSTALLED_APPS, update WAGTAIL_SITE_NAME |
| `vcard_project/urls.py` | Add `path('', include(wagtail_urls))` catch-all at end |
| `.gitignore` | Add `uv.lock` note (keep committed), add `db_dev.sqlite3` to ignore |

---

## PHASE 1 — Foundation (sequential, must complete before other phases)

---

### Task 1: Create dev branch

**Files:** none (git only)

- [ ] **Step 1: Verify git status is clean**
  ```bash
  cd /Users/hosammulla/Development/VCARD/vcard
  git status
  ```
  Expected: `nothing to commit, working tree clean`. If not, commit or stash changes first.

- [ ] **Step 2: Create and push dev branch**
  ```bash
  git checkout -b dev
  git push -u origin dev
  ```
  Expected: Branch `dev` created and tracking `origin/dev`.

- [ ] **Step 3: Confirm**
  ```bash
  git branch -vv
  ```
  Expected: `* dev` with `[origin/dev]` tracking info.

---

### Task 2: Install uv and migrate to pyproject.toml

**Files:**
- Create: `pyproject.toml`
- Create: `.python-version`
- Delete: `requirements.txt`
- Delete: `requirements-local.txt`

- [ ] **Step 1: Install uv**
  ```bash
  curl -LsSf https://astral.sh/uv/install.sh | sh
  source $HOME/.local/bin/env
  uv --version
  ```
  Expected: `uv 0.x.x` printed.

- [ ] **Step 2: Create `.python-version`**
  ```
  3.12
  ```

- [ ] **Step 3: Create `pyproject.toml`**
  ```toml
  [project]
  name = "vcard"
  version = "0.1.0"
  requires-python = ">=3.12"
  dependencies = [
      "django>=5.2,<6",
      "djangorestframework>=3.17",
      "wagtail>=6.4,<7",
      "pillow>=12",
      "qrcode[pil]>=8.2",
      "python-decouple>=3.8",
      "psycopg2-binary>=2.9.12",
      "vobject>=0.9.6",
      "gunicorn>=26",
      "whitenoise>=6.7",
      "requests>=2.34",
      "django-crispy-forms>=2.6",
      "crispy-bootstrap5>=2026.3",
      "python-slugify>=8",
      "stripe>=11",
      "segno>=1.6.6",
      "ua-parser>=1.0.1",
  ]

  [dependency-groups]
  dev = [
      "python-dotenv>=1.2",
  ]

  [tool.uv]
  dev-dependencies = []
  ```

- [ ] **Step 4: Create uv venv and sync**
  ```bash
  uv venv
  uv sync
  ```
  Expected: `.venv/` created, all packages resolved and installed, `uv.lock` generated.

- [ ] **Step 5: Verify Django still starts**
  ```bash
  uv run python manage.py check
  ```
  Expected: `System check identified no issues (0 silenced).`

- [ ] **Step 6: Delete old requirements files**
  ```bash
  rm requirements.txt requirements-local.txt
  ```

- [ ] **Step 7: Update .gitignore**
  Add these lines to `.gitignore`:
  ```
  .venv/
  db_dev.sqlite3
  ```
  Note: `uv.lock` should NOT be in .gitignore — commit it for reproducible builds.

- [ ] **Step 8: Commit**
  ```bash
  git add pyproject.toml uv.lock .python-version .gitignore
  git rm requirements.txt requirements-local.txt
  git commit -m "chore: migrate to uv + pyproject.toml"
  ```

---

### Task 3: Update packages to latest compatible versions

**Files:**
- Modify: `pyproject.toml` (already pinned to latest in Task 2)

- [ ] **Step 1: Check what uv resolved**
  ```bash
  uv sync
  uv run python -c "import django; print(django.__version__)"
  uv run python -c "import wagtail; print(wagtail.__version__)"
  ```
  Expected: Django 5.2.x, Wagtail 6.4.x printed.

- [ ] **Step 2: Run Django system check**
  ```bash
  uv run python manage.py check
  ```
  Expected: No issues. If deprecation warnings appear for removed features, note them — they'll need fixing.

- [ ] **Step 3: Run existing migrations to confirm DB still works**
  ```bash
  uv run python manage.py migrate
  ```
  Expected: `No migrations to apply.`

- [ ] **Step 4: Start server and confirm app loads**
  ```bash
  uv run python manage.py runserver &
  sleep 3
  curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/login/
  kill %1
  ```
  Expected: `200`

- [ ] **Step 5: Commit**
  ```bash
  git add uv.lock
  git commit -m "chore: update uv.lock with resolved package versions"
  ```

---

## PHASE 2 — Wagtail CMS App (sequential, must complete before Phase 3)

---

### Task 4: Create cms app skeleton + register with Django

**Files:**
- Create: `cms/__init__.py`
- Create: `cms/apps.py`
- Modify: `vcard_project/settings.py`
- Modify: `vcard_project/urls.py`

- [ ] **Step 1: Create app directory and files**
  ```bash
  mkdir -p cms/migrations
  touch cms/__init__.py cms/migrations/__init__.py
  ```

- [ ] **Step 2: Create `cms/apps.py`**
  ```python
  from django.apps import AppConfig

  class CmsConfig(AppConfig):
      default_auto_field = 'django.db.models.BigAutoField'
      name = 'cms'
      verbose_name = 'CMS'
  ```

- [ ] **Step 3: Create empty `cms/models.py`** (will be filled in Task 5)
  ```python
  # Page models defined here — see blocks.py for StreamField blocks
  ```

- [ ] **Step 4: Add `cms` to INSTALLED_APPS in `vcard_project/settings.py`**

  Find the line `'wagtail',` and add `'cms'` after it:
  ```python
  INSTALLED_APPS = [
      ...
      'wagtail',
      'modelcluster',
      'taggit',
      # Project apps
      'cards',
      'cms',   # ← add this
      ...
  ]
  ```
  Also update WAGTAIL_SITE_NAME:
  ```python
  WAGTAIL_SITE_NAME = 'VCard'
  ```

- [ ] **Step 5: Add Wagtail catch-all to `vcard_project/urls.py`**

  The current `urls.py` already imports `wagtail_urls`. Add the catch-all **after** `cards.urls`:
  ```python
  urlpatterns = [
      path('admin/', admin.site.urls),
      path('cms/', include(wagtailadmin_urls)),
      path('cms/documents/', include(wagtaildocs_urls)),
      path('', include('cards.urls')),       # SaaS routes — must come before wagtail catch-all
      path('', include(wagtail_urls)),        # Wagtail pages — catch-all, must be last
  ]
  ```

- [ ] **Step 6: Verify system check passes**
  ```bash
  uv run python manage.py check
  ```
  Expected: No issues.

- [ ] **Step 7: Commit**
  ```bash
  git add cms/ vcard_project/settings.py vcard_project/urls.py
  git commit -m "feat: add cms app skeleton and wire up Wagtail catch-all URL"
  ```

---

### Task 5: Write StreamField blocks

**Files:**
- Create: `cms/blocks.py`
- Create: `tests/test_cms_pages.py` (first test only)

- [ ] **Step 1: Write the first failing test**
  Create `tests/__init__.py` (empty) and `tests/test_cms_pages.py`:
  ```python
  from django.test import TestCase
  from cms.blocks import FeatureBlock, StepBlock, TestimonialBlock, PricingTierBlock, FAQBlock

  class BlockImportTest(TestCase):
      def test_all_blocks_importable(self):
          self.assertIsNotNone(FeatureBlock)
          self.assertIsNotNone(StepBlock)
          self.assertIsNotNone(TestimonialBlock)
          self.assertIsNotNone(PricingTierBlock)
          self.assertIsNotNone(FAQBlock)
  ```

- [ ] **Step 2: Run test to confirm it fails**
  ```bash
  uv run python manage.py test tests.test_cms_pages.BlockImportTest -v 2
  ```
  Expected: `ImportError: cannot import name 'FeatureBlock' from 'cms.blocks'`

- [ ] **Step 3: Create `cms/blocks.py`**
  ```python
  from wagtail.blocks import (
      CharBlock, TextBlock, RichTextBlock, StructBlock, ListBlock, BooleanBlock,
  )
  from wagtail.images.blocks import ImageChooserBlock


  class FeatureBlock(StructBlock):
      icon = CharBlock(max_length=10, help_text='Emoji or single character icon')
      title = CharBlock(max_length=100)
      description = TextBlock()

      class Meta:
          icon = 'placeholder'
          label = 'Feature'


  class StepBlock(StructBlock):
      title = CharBlock(max_length=100)
      description = TextBlock()

      class Meta:
          icon = 'order'
          label = 'Step'


  class TestimonialBlock(StructBlock):
      quote = TextBlock()
      author_name = CharBlock(max_length=100)
      author_role = CharBlock(max_length=100)
      avatar = ImageChooserBlock(required=False)

      class Meta:
          icon = 'user'
          label = 'Testimonial'


  class PricingTierBlock(StructBlock):
      name = CharBlock(max_length=50)
      price = CharBlock(max_length=20, help_text='e.g. $9.99')
      period = CharBlock(max_length=20, default='/mo')
      description = CharBlock(max_length=200)
      features = ListBlock(CharBlock(max_length=200))
      is_popular = BooleanBlock(required=False, default=False)
      cta_label = CharBlock(max_length=50)
      cta_url = CharBlock(max_length=200)

      class Meta:
          icon = 'pick'
          label = 'Pricing Tier'


  class FAQBlock(StructBlock):
      question = CharBlock(max_length=300)
      answer = RichTextBlock()

      class Meta:
          icon = 'help'
          label = 'FAQ Item'
  ```

- [ ] **Step 4: Run test to confirm it passes**
  ```bash
  uv run python manage.py test tests.test_cms_pages.BlockImportTest -v 2
  ```
  Expected: `OK`

- [ ] **Step 5: Commit**
  ```bash
  git add cms/blocks.py tests/
  git commit -m "feat: add Wagtail StreamField blocks"
  ```

---

### Task 6: Write Wagtail page models

**Files:**
- Modify: `cms/models.py`
- Modify: `tests/test_cms_pages.py`

- [ ] **Step 1: Add page model tests**
  Append to `tests/test_cms_pages.py`:
  ```python
  from wagtail.test.utils import WagtailPageTests
  from cms.models import (
      HomePage, BlogIndexPage, BlogPostPage, AboutPage, PricingPage, LegalPage
  )

  class HomePageTests(WagtailPageTests):
      def test_can_create_under_root(self):
          self.assertCanCreateAt(Page, HomePage)

      def test_can_create_blog_index_under_home(self):
          self.assertCanCreateAt(HomePage, BlogIndexPage)

      def test_can_create_about_under_home(self):
          self.assertCanCreateAt(HomePage, AboutPage)

      def test_can_create_pricing_under_home(self):
          self.assertCanCreateAt(HomePage, PricingPage)

      def test_can_create_legal_under_home(self):
          self.assertCanCreateAt(HomePage, LegalPage)

      def test_blog_post_only_under_blog_index(self):
          self.assertCanCreateAt(BlogIndexPage, BlogPostPage)
          self.assertCanNotCreateAt(HomePage, BlogPostPage)
  ```

- [ ] **Step 2: Run tests to confirm they fail**
  ```bash
  uv run python manage.py test tests.test_cms_pages -v 2
  ```
  Expected: `ImportError: cannot import name 'HomePage' from 'cms.models'`

- [ ] **Step 3: Write `cms/models.py`**
  ```python
  import datetime
  from django.db import models
  from wagtail.models import Page
  from wagtail.fields import RichTextField, StreamField
  from wagtail.admin.panels import FieldPanel, MultiFieldPanel
  from wagtail.search import index
  from .blocks import FeatureBlock, StepBlock, TestimonialBlock, PricingTierBlock, FAQBlock
  from wagtail.blocks import RichTextBlock, CharBlock
  from wagtail.images.blocks import ImageChooserBlock


  class HomePage(Page):
      hero_badge_text = models.CharField(max_length=200, blank=True)
      hero_heading = models.CharField(max_length=300, default='Your digital identity, one tap away')
      hero_subtext = models.TextField(blank=True)
      hero_primary_cta_label = models.CharField(max_length=100, default="Create your card — it's free")
      hero_secondary_cta_label = models.CharField(max_length=100, default='See how it works →')
      hero_social_proof_text = models.CharField(max_length=200, blank=True, default='12,000+ professionals already use VCard')

      features = StreamField([('feature', FeatureBlock())], blank=True, use_json_field=True)
      steps = StreamField([('step', StepBlock())], blank=True, use_json_field=True)
      testimonials = StreamField([('testimonial', TestimonialBlock())], blank=True, use_json_field=True)

      cta_heading = models.CharField(max_length=200, blank=True, default='Ready to make your mark?')
      cta_subtext = models.CharField(max_length=300, blank=True, default='Join 12,000+ professionals who\'ve gone digital. Free to start, no credit card required.')
      cta_button_label = models.CharField(max_length=100, default='Start your free card →')

      content_panels = Page.content_panels + [
          MultiFieldPanel([
              FieldPanel('hero_badge_text'),
              FieldPanel('hero_heading'),
              FieldPanel('hero_subtext'),
              FieldPanel('hero_primary_cta_label'),
              FieldPanel('hero_secondary_cta_label'),
              FieldPanel('hero_social_proof_text'),
          ], heading='Hero Section'),
          FieldPanel('features'),
          FieldPanel('steps'),
          FieldPanel('testimonials'),
          MultiFieldPanel([
              FieldPanel('cta_heading'),
              FieldPanel('cta_subtext'),
              FieldPanel('cta_button_label'),
          ], heading='CTA Banner'),
      ]

      subpage_types = ['cms.BlogIndexPage', 'cms.AboutPage', 'cms.PricingPage', 'cms.LegalPage']

      class Meta:
          verbose_name = 'Home Page'


  class BlogIndexPage(Page):
      intro = RichTextField(blank=True)

      content_panels = Page.content_panels + [
          FieldPanel('intro'),
      ]

      parent_page_types = ['cms.HomePage']
      subpage_types = ['cms.BlogPostPage']

      def get_context(self, request):
          context = super().get_context(request)
          from django.core.paginator import Paginator
          posts = BlogPostPage.objects.child_of(self).live().order_by('-published_date')
          paginator = Paginator(posts, 10)
          context['posts'] = paginator.get_page(request.GET.get('page', 1))
          return context

      class Meta:
          verbose_name = 'Blog'


  class BlogPostPage(Page):
      author = models.CharField(max_length=200)
      published_date = models.DateField(default=datetime.date.today)
      hero_image = models.ForeignKey(
          'wagtailimages.Image', null=True, blank=True,
          on_delete=models.SET_NULL, related_name='+',
      )
      excerpt = models.TextField(blank=True, max_length=500)
      body = StreamField([
          ('paragraph', RichTextBlock()),
          ('heading', CharBlock(form_classname='title')),
          ('image', ImageChooserBlock()),
      ], use_json_field=True)

      content_panels = Page.content_panels + [
          FieldPanel('author'),
          FieldPanel('published_date'),
          FieldPanel('hero_image'),
          FieldPanel('excerpt'),
          FieldPanel('body'),
      ]

      search_fields = Page.search_fields + [
          index.SearchField('body'),
          index.SearchField('excerpt'),
      ]

      parent_page_types = ['cms.BlogIndexPage']
      subpage_types = []

      class Meta:
          verbose_name = 'Blog Post'


  class AboutPage(Page):
      body = StreamField([
          ('paragraph', RichTextBlock()),
          ('heading', CharBlock()),
          ('image', ImageChooserBlock()),
      ], use_json_field=True, blank=True)

      content_panels = Page.content_panels + [
          FieldPanel('body'),
      ]

      parent_page_types = ['cms.HomePage']
      subpage_types = []

      class Meta:
          verbose_name = 'About Page'


  class PricingPage(Page):
      heading = models.CharField(max_length=200, default='Simple, transparent pricing')
      subtext = models.CharField(max_length=400, blank=True, default='Start free. Upgrade when you\'re ready.')
      tiers = StreamField([('tier', PricingTierBlock())], blank=True, use_json_field=True)
      faq = StreamField([('faq', FAQBlock())], blank=True, use_json_field=True)

      content_panels = Page.content_panels + [
          FieldPanel('heading'),
          FieldPanel('subtext'),
          FieldPanel('tiers'),
          FieldPanel('faq'),
      ]

      parent_page_types = ['cms.HomePage']
      subpage_types = []

      class Meta:
          verbose_name = 'Pricing Page'


  class LegalPage(Page):
      body = RichTextField()

      content_panels = Page.content_panels + [
          FieldPanel('body'),
      ]

      parent_page_types = ['cms.HomePage']
      subpage_types = []

      class Meta:
          verbose_name = 'Legal Page'
  ```

- [ ] **Step 4: Create and run migrations**
  ```bash
  uv run python manage.py makemigrations cms
  uv run python manage.py migrate
  ```
  Expected: `cms/migrations/0001_initial.py` created, migrations applied.

- [ ] **Step 5: Run tests**
  ```bash
  uv run python manage.py test tests.test_cms_pages -v 2
  ```
  Expected: All 6 tests pass.

- [ ] **Step 6: Commit**
  ```bash
  git add cms/models.py cms/migrations/ tests/test_cms_pages.py
  git commit -m "feat: add Wagtail page models (HomePage, Blog, About, Pricing, Legal)"
  ```

---

### Task 7: Seed the initial page tree

**Files:**
- Create: `cms/migrations/0002_initial_pages.py`

- [ ] **Step 1: Create the data migration**
  ```bash
  uv run python manage.py makemigrations cms --empty --name=initial_pages
  ```

- [ ] **Step 2: Write the migration**
  Open `cms/migrations/0002_initial_pages.py` and replace its contents with:
  ```python
  from django.db import migrations


  def create_page_tree(apps, schema_editor):
      from wagtail.models import Page, Site
      from cms.models import HomePage, BlogIndexPage, AboutPage, PricingPage, LegalPage

      # Delete the default Wagtail root page welcome page if it exists
      Page.objects.filter(depth=2).delete()

      # Get the Wagtail root
      root = Page.objects.get(depth=1)

      # Create Home page
      home = HomePage(
          title='VCard — Digital Business Cards',
          slug='home',
          hero_heading='Your digital identity, one tap away',
          hero_badge_text='✦ NFC-ready digital business cards',
          hero_subtext='Create a stunning digital business card in minutes. Share via NFC, QR code, or link. Track every interaction with real-time analytics.',
          cta_heading='Ready to make your mark?',
          cta_subtext="Join 12,000+ professionals who've gone digital. Free to start, no credit card required.",
          cta_button_label='Start your free card →',
          show_in_menus=True,
      )
      root.add_child(instance=home)

      # Update or create Wagtail Site pointing to home
      site, created = Site.objects.get_or_create(
          hostname='localhost',
          defaults={'root_page': home, 'site_name': 'VCard', 'is_default_site': True, 'port': 80},
      )
      if not created:
          site.root_page = home
          site.site_name = 'VCard'
          site.is_default_site = True
          site.save()

      # Blog
      blog = BlogIndexPage(title='Blog', slug='blog', show_in_menus=True)
      home.add_child(instance=blog)

      # About
      about = AboutPage(title='About', slug='about', show_in_menus=True)
      home.add_child(instance=about)

      # Pricing
      pricing = PricingPage(
          title='Pricing',
          slug='pricing',
          heading='Simple, transparent pricing',
          subtext='Start free. Upgrade when you\'re ready.',
          show_in_menus=True,
      )
      home.add_child(instance=pricing)

      # Privacy Policy
      privacy = LegalPage(
          title='Privacy Policy',
          slug='privacy',
          body='<p>Privacy policy content coming soon.</p>',
      )
      home.add_child(instance=privacy)

      # Terms of Service
      terms = LegalPage(
          title='Terms of Service',
          slug='terms',
          body='<p>Terms of service content coming soon.</p>',
      )
      home.add_child(instance=terms)


  def delete_page_tree(apps, schema_editor):
      from wagtail.models import Page
      Page.objects.filter(depth__gte=2).delete()


  class Migration(migrations.Migration):
      dependencies = [
          ('cms', '0001_initial'),
          ('wagtailcore', '0094_alter_page_locale'),
      ]

      operations = [
          migrations.RunPython(create_page_tree, delete_page_tree),
      ]
  ```

- [ ] **Step 3: Run the migration**
  ```bash
  uv run python manage.py migrate cms 0002
  ```
  Expected: `Applying cms.0002_initial_pages... OK`

- [ ] **Step 4: Verify pages exist**
  ```bash
  uv run python manage.py shell -c "
  from wagtail.models import Page
  for p in Page.objects.all().order_by('path'):
      print('  ' * (p.depth - 1) + p.title, '->', p.url_path)
  "
  ```
  Expected: Home, Blog, About, Pricing, Privacy Policy, Terms of Service listed.

- [ ] **Step 5: Verify / resolves to Wagtail homepage**
  ```bash
  uv run python manage.py runserver &
  sleep 3
  curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/
  kill %1
  ```
  Expected: `200` (Wagtail serves the page, though template doesn't exist yet — that's fine, 500 means the page was found, 404 means routing is broken)

- [ ] **Step 6: Commit**
  ```bash
  git add cms/migrations/0002_initial_pages.py
  git commit -m "feat: seed initial Wagtail page tree (home, blog, about, pricing, legal)"
  ```

---

## PHASE 3 — Templates (Tasks 8–12 can run in parallel once Task 7 is complete)

---

### Task 8: Build shared marketing base template (cms/base.html)

**Files:**
- Create: `cms/templates/cms/base.html`

- [ ] **Step 1: Create template directory**
  ```bash
  mkdir -p cms/templates/cms
  ```

- [ ] **Step 2: Create `cms/templates/cms/base.html`**
  ```html
  <!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}VCard{% endblock %} — Digital Business Cards</title>
    <meta name="description" content="{% block meta_description %}Create and share digital business cards via NFC and QR code with real-time analytics.{% endblock %}">
    <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
      body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; color: #111827; background: #fff; line-height: 1.5; font-size: 16px; }
      a { color: inherit; text-decoration: none; }

      /* NAV */
      .site-nav { display: flex; justify-content: space-between; align-items: center; padding: 16px 48px; border-bottom: 1px solid #f3f4f6; position: sticky; top: 0; background: rgba(255,255,255,0.95); backdrop-filter: blur(8px); z-index: 100; }
      .nav-logo { font-weight: 800; font-size: 18px; letter-spacing: -0.5px; }
      .nav-logo span { color: #6366f1; }
      .nav-links { display: flex; gap: 28px; }
      .nav-links a { color: #6b7280; font-size: 14px; font-weight: 500; transition: color .15s; }
      .nav-links a:hover { color: #111827; }
      .nav-actions { display: flex; gap: 12px; align-items: center; }
      .btn-ghost { color: #374151; font-size: 14px; font-weight: 500; cursor: pointer; }
      .btn-dark { background: #111827; color: #fff; font-size: 14px; font-weight: 600; padding: 9px 20px; border-radius: 8px; transition: background .15s; }
      .btn-dark:hover { background: #1f2937; }

      /* FOOTER */
      .site-footer { padding: 56px 48px 32px; border-top: 1px solid #f3f4f6; }
      .footer-top { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 48px; margin-bottom: 48px; }
      .footer-brand-logo { font-weight: 800; font-size: 16px; margin-bottom: 10px; }
      .footer-brand-logo span { color: #6366f1; }
      .footer-brand p { color: #9ca3af; font-size: 13px; line-height: 1.6; max-width: 200px; }
      .footer-col h4 { font-size: 13px; font-weight: 700; color: #111827; margin-bottom: 14px; }
      .footer-col a { display: block; color: #9ca3af; font-size: 13px; margin-bottom: 10px; transition: color .15s; }
      .footer-col a:hover { color: #374151; }
      .footer-bottom { display: flex; justify-content: space-between; align-items: center; padding-top: 24px; border-top: 1px solid #f3f4f6; }
      .footer-copyright { color: #d1d5db; font-size: 12px; }
      .footer-socials { display: flex; gap: 10px; }
      .social-icon { width: 30px; height: 30px; border-radius: 6px; background: #f3f4f6; display: flex; align-items: center; justify-content: center; font-size: 13px; color: #9ca3af; transition: background .15s; }
      .social-icon:hover { background: #e5e7eb; }

      /* UTILITIES */
      .container { max-width: 1040px; margin: 0 auto; padding: 0 48px; }
      .section-label { color: #6366f1; font-size: 12px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 12px; }
      .section-title { font-size: 34px; font-weight: 800; letter-spacing: -0.5px; line-height: 1.2; margin-bottom: 14px; }
      .section-sub { color: #6b7280; font-size: 16px; line-height: 1.6; max-width: 500px; margin-bottom: 44px; }
      .btn-primary { background: #111827; color: #fff; font-size: 15px; font-weight: 600; padding: 13px 26px; border-radius: 10px; display: inline-block; transition: background .15s; }
      .btn-primary:hover { background: #1f2937; }
      .btn-outline { border: 1px solid #e5e7eb; color: #374151; font-size: 15px; font-weight: 500; padding: 13px 26px; border-radius: 10px; display: inline-block; transition: border-color .15s; }
      .btn-outline:hover { border-color: #d1d5db; }

      @media (max-width: 768px) {
        .site-nav { padding: 14px 20px; }
        .nav-links { display: none; }
        .footer-top { grid-template-columns: 1fr 1fr; gap: 24px; }
        .container { padding: 0 20px; }
        .section-title { font-size: 26px; }
      }
    </style>
    {% block extra_css %}{% endblock %}
  </head>
  <body>

  <nav class="site-nav">
    <a href="/" class="nav-logo">V<span>Card</span></a>
    <div class="nav-links">
      <a href="/#features">Features</a>
      <a href="/pricing/">Pricing</a>
      <a href="/blog/">Blog</a>
      <a href="/about/">About</a>
    </div>
    <div class="nav-actions">
      <a href="/login/" class="btn-ghost">Sign In</a>
      <a href="/register/" class="btn-dark">Get Started →</a>
    </div>
  </nav>

  {% block content %}{% endblock %}

  <footer class="site-footer">
    <div class="footer-top">
      <div class="footer-brand">
        <div class="footer-brand-logo">V<span>Card</span></div>
        <p>The modern digital business card platform for professionals and teams.</p>
      </div>
      <div class="footer-col">
        <h4>Product</h4>
        <a href="/#features">Features</a>
        <a href="/pricing/">Pricing</a>
        <a href="/blog/">Blog</a>
      </div>
      <div class="footer-col">
        <h4>Company</h4>
        <a href="/about/">About</a>
        <a href="mailto:hello@hosammo.com">Contact</a>
      </div>
      <div class="footer-col">
        <h4>Legal</h4>
        <a href="/privacy/">Privacy Policy</a>
        <a href="/terms/">Terms of Service</a>
      </div>
    </div>
    <div class="footer-bottom">
      <p class="footer-copyright">© {% now "Y" %} VCard. All rights reserved.</p>
      <div class="footer-socials">
        <a href="#" class="social-icon">𝕏</a>
        <a href="#" class="social-icon">in</a>
      </div>
    </div>
  </footer>

  {% block extra_js %}{% endblock %}
  </body>
  </html>
  ```

- [ ] **Step 3: Verify template is found by Django**
  ```bash
  uv run python manage.py shell -c "
  from django.template.loader import get_template
  t = get_template('cms/base.html')
  print('Template found:', t.origin.name)
  "
  ```
  Expected: Path to `cms/templates/cms/base.html` printed.

- [ ] **Step 4: Commit**
  ```bash
  git add cms/templates/
  git commit -m "feat: add cms marketing base template with nav and footer"
  ```

---

### Task 9: Build homepage template

**Files:**
- Create: `cms/templates/cms/home_page.html`

- [ ] **Step 1: Add URL test for homepage**
  Append to `tests/test_cms_pages.py`:
  ```python
  from django.test import TestCase as DjangoTestCase

  class HomePageURLTest(DjangoTestCase):
      def test_homepage_returns_200(self):
          response = self.client.get('/')
          self.assertEqual(response.status_code, 200)

      def test_homepage_contains_brand(self):
          response = self.client.get('/')
          self.assertContains(response, 'VCard')
  ```

- [ ] **Step 2: Run tests — expect failure (template missing)**
  ```bash
  uv run python manage.py test tests.test_cms_pages.HomePageURLTest -v 2
  ```
  Expected: `TemplateDoesNotExist: cms/home_page.html`

- [ ] **Step 3: Create `cms/templates/cms/home_page.html`**
  ```html
  {% extends "cms/base.html" %}
  {% load wagtailcore_tags wagtailimages_tags %}

  {% block title %}{{ page.seo_title|default:"VCard — Digital Business Cards" }}{% endblock %}

  {% block content %}

  {# HERO #}
  <section style="padding: 88px 48px 72px; text-align: center;">
    <div style="max-width: 720px; margin: 0 auto;">
      {% if page.hero_badge_text %}
      <div style="display:inline-flex;align-items:center;gap:6px;background:#f3f4f6;border:1px solid #e5e7eb;color:#6b7280;font-size:13px;padding:5px 16px;border-radius:20px;margin-bottom:28px;font-weight:500;">
        {{ page.hero_badge_text }}
      </div>
      {% endif %}
      <h1 style="font-size:52px;font-weight:800;line-height:1.15;letter-spacing:-1.5px;color:#111827;margin-bottom:20px;">
        {{ page.hero_heading|default:"Your digital identity,"|linebreaksbr }}
      </h1>
      <p style="color:#6b7280;font-size:18px;line-height:1.6;max-width:520px;margin:0 auto 36px;">
        {{ page.hero_subtext }}
      </p>
      <div style="display:flex;gap:14px;justify-content:center;flex-wrap:wrap;margin-bottom:44px;">
        <a href="/register/" class="btn-primary">{{ page.hero_primary_cta_label }}</a>
        <a href="#how-it-works" class="btn-outline">{{ page.hero_secondary_cta_label }}</a>
      </div>
      {% if page.hero_social_proof_text %}
      <p style="color:#9ca3af;font-size:13px;">{{ page.hero_social_proof_text }}</p>
      {% endif %}
    </div>
  </section>

  {# FEATURES #}
  {% if page.features %}
  <section id="features" style="padding: 72px 48px; background: #f9fafb;">
    <div class="container" style="padding: 0;">
      <div class="section-label">Features</div>
      <div class="section-title">Everything you need to make a lasting first impression</div>
      <div class="section-sub">From NFC sharing to team management and analytics — all in one place.</div>
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:20px;">
        {% for block in page.features %}
        <div style="background:#fff;border:1px solid #f3f4f6;border-radius:14px;padding:28px;">
          <div style="width:44px;height:44px;border-radius:10px;background:#eff6ff;display:flex;align-items:center;justify-content:center;font-size:20px;margin-bottom:14px;">{{ block.value.icon }}</div>
          <h3 style="font-size:15px;font-weight:700;margin-bottom:6px;color:#111827;">{{ block.value.title }}</h3>
          <p style="color:#9ca3af;font-size:13px;line-height:1.6;">{{ block.value.description }}</p>
        </div>
        {% endfor %}
      </div>
    </div>
  </section>
  {% endif %}

  {# HOW IT WORKS #}
  {% if page.steps %}
  <section id="how-it-works" style="padding: 72px 48px;">
    <div class="container" style="padding: 0;">
      <div class="section-label">How it works</div>
      <div class="section-title">Up and running in 3 steps</div>
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:40px;margin-top:40px;position:relative;">
        {% for block in page.steps %}
        <div style="text-align:center;">
          <div style="width:44px;height:44px;border-radius:50%;background:#111827;color:#fff;font-weight:800;font-size:16px;display:flex;align-items:center;justify-content:center;margin:0 auto 18px;">{{ forloop.counter }}</div>
          <h3 style="font-size:15px;font-weight:700;margin-bottom:8px;">{{ block.value.title }}</h3>
          <p style="color:#9ca3af;font-size:13px;line-height:1.6;">{{ block.value.description }}</p>
        </div>
        {% endfor %}
      </div>
    </div>
  </section>
  {% endif %}

  {# PRICING PREVIEW #}
  <section style="padding: 72px 48px; background: #f9fafb;">
    <div class="container" style="padding: 0;">
      <div class="section-label">Pricing</div>
      <div class="section-title">Simple, transparent pricing</div>
      <div class="section-sub">Start free. Upgrade when you're ready.</div>
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:20px;">
        {# Free #}
        <div style="background:#fff;border:1px solid #e5e7eb;border-radius:16px;padding:32px;">
          <div style="font-size:12px;font-weight:700;color:#9ca3af;text-transform:uppercase;letter-spacing:1px;margin-bottom:8px;">Free</div>
          <div style="font-size:40px;font-weight:800;letter-spacing:-1px;margin-bottom:4px;">$0<span style="font-size:15px;font-weight:500;color:#9ca3af;">/mo</span></div>
          <p style="color:#9ca3af;font-size:13px;margin-bottom:20px;">Perfect for getting started</p>
          <a href="/register/" style="display:block;text-align:center;padding:10px;border-radius:8px;font-size:14px;font-weight:600;border:1px solid #e5e7eb;color:#374151;margin-bottom:20px;">Get started free</a>
          <ul style="list-style:none;display:flex;flex-direction:column;gap:10px;">
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>1 digital business card</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>QR code sharing</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>Basic analytics (7 days)</li>
          </ul>
        </div>
        {# Pro — highlighted #}
        <div style="background:#fff;border:2px solid #6366f1;border-radius:16px;padding:32px;position:relative;box-shadow:0 0 0 1px #6366f1;">
          <div style="position:absolute;top:-13px;left:50%;transform:translateX(-50%);background:#6366f1;color:#fff;font-size:11px;font-weight:700;padding:4px 14px;border-radius:20px;white-space:nowrap;">Most Popular</div>
          <div style="font-size:12px;font-weight:700;color:#9ca3af;text-transform:uppercase;letter-spacing:1px;margin-bottom:8px;">Pro</div>
          <div style="font-size:40px;font-weight:800;letter-spacing:-1px;margin-bottom:4px;">$9<span style="font-size:15px;font-weight:500;color:#9ca3af;">.99/mo</span></div>
          <p style="color:#9ca3af;font-size:13px;margin-bottom:20px;">For active networkers</p>
          <a href="/register/?plan=pro" style="display:block;text-align:center;padding:10px;border-radius:8px;font-size:14px;font-weight:600;background:#111827;color:#fff;margin-bottom:20px;">Start Pro trial</a>
          <ul style="list-style:none;display:flex;flex-direction:column;gap:10px;">
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>5 business cards</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>NFC tag support</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>Advanced analytics (90 days)</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>Lead capture</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>Remove branding</li>
          </ul>
        </div>
        {# Business #}
        <div style="background:#fff;border:1px solid #e5e7eb;border-radius:16px;padding:32px;">
          <div style="font-size:12px;font-weight:700;color:#9ca3af;text-transform:uppercase;letter-spacing:1px;margin-bottom:8px;">Business</div>
          <div style="font-size:40px;font-weight:800;letter-spacing:-1px;margin-bottom:4px;">$29<span style="font-size:15px;font-weight:500;color:#9ca3af;">.99/mo</span></div>
          <p style="color:#9ca3af;font-size:13px;margin-bottom:20px;">For teams & companies</p>
          <a href="/register/?plan=business" style="display:block;text-align:center;padding:10px;border-radius:8px;font-size:14px;font-weight:600;border:1px solid #e5e7eb;color:#374151;margin-bottom:20px;">Get started</a>
          <ul style="list-style:none;display:flex;flex-direction:column;gap:10px;">
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>Unlimited cards</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>Team management (10 seats)</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>Lifetime analytics</li>
            <li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>CSV export</li>
          </ul>
        </div>
      </div>
      <div style="text-align:center;margin-top:28px;">
        <a href="/pricing/" style="color:#6366f1;font-size:14px;font-weight:600;">See full comparison →</a>
      </div>
    </div>
  </section>

  {# TESTIMONIALS #}
  {% if page.testimonials %}
  <section style="padding: 72px 48px;">
    <div class="container" style="padding: 0;">
      <div class="section-label">Testimonials</div>
      <div class="section-title">Loved by professionals worldwide</div>
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:20px;margin-top:40px;">
        {% for block in page.testimonials %}
        <div style="background:#f9fafb;border:1px solid #f3f4f6;border-radius:14px;padding:28px;">
          <div style="color:#f59e0b;font-size:13px;margin-bottom:14px;">★★★★★</div>
          <p style="font-size:14px;color:#374151;line-height:1.65;margin-bottom:18px;">"{{ block.value.quote }}"</p>
          <div style="display:flex;align-items:center;gap:12px;">
            {% if block.value.avatar %}{% image block.value.avatar fill-32x32-c100 style="border-radius:50%;" %}{% else %}<div style="width:34px;height:34px;border-radius:50%;background:#e5e7eb;"></div>{% endif %}
            <div>
              <div style="font-size:13px;font-weight:700;color:#111827;">{{ block.value.author_name }}</div>
              <div style="font-size:12px;color:#9ca3af;">{{ block.value.author_role }}</div>
            </div>
          </div>
        </div>
        {% endfor %}
      </div>
    </div>
  </section>
  {% endif %}

  {# CTA BANNER #}
  <section style="padding: 80px 48px; background: #111827; text-align: center;">
    <h2 style="font-size:38px;font-weight:800;color:#fff;letter-spacing:-0.5px;margin-bottom:14px;">{{ page.cta_heading }}</h2>
    <p style="color:#9ca3af;font-size:16px;margin-bottom:32px;">{{ page.cta_subtext }}</p>
    <a href="/register/" style="background:#fff;color:#111827;font-size:15px;font-weight:700;padding:14px 30px;border-radius:10px;display:inline-block;">{{ page.cta_button_label }}</a>
    <p style="color:#6b7280;font-size:13px;margin-top:14px;">Free forever · No credit card needed · Setup in 5 minutes</p>
  </section>

  {% endblock %}
  ```

- [ ] **Step 4: Run tests**
  ```bash
  uv run python manage.py test tests.test_cms_pages.HomePageURLTest -v 2
  ```
  Expected: Both tests pass.

- [ ] **Step 5: Visually verify homepage**
  ```bash
  uv run python manage.py runserver
  ```
  Open `http://127.0.0.1:8000/` — confirm hero, features placeholder (empty until CMS content added), pricing tiers, CTA banner, footer all render correctly.

- [ ] **Step 6: Commit**
  ```bash
  git add cms/templates/cms/home_page.html
  git commit -m "feat: build B-style homepage template with all 9 sections"
  ```

---

### Task 10: Build blog templates

**Files:**
- Create: `cms/templates/cms/blog_index_page.html`
- Create: `cms/templates/cms/blog_post_page.html`

- [ ] **Step 1: Add blog URL tests**
  Append to `tests/test_cms_pages.py`:
  ```python
  class BlogURLTest(DjangoTestCase):
      def test_blog_index_returns_200(self):
          response = self.client.get('/blog/')
          self.assertEqual(response.status_code, 200)

      def test_blog_contains_heading(self):
          response = self.client.get('/blog/')
          self.assertContains(response, 'Blog')
  ```

- [ ] **Step 2: Run to confirm failure**
  ```bash
  uv run python manage.py test tests.test_cms_pages.BlogURLTest -v 2
  ```
  Expected: `TemplateDoesNotExist: cms/blog_index_page.html`

- [ ] **Step 3: Create `cms/templates/cms/blog_index_page.html`**
  ```html
  {% extends "cms/base.html" %}
  {% load wagtailcore_tags wagtailimages_tags %}

  {% block title %}Blog{% endblock %}

  {% block content %}
  <section style="padding: 72px 48px;">
    <div class="container" style="padding: 0;">
      <div class="section-label">Blog</div>
      <h1 class="section-title">Articles & Updates</h1>
      {% if page.intro %}<div style="color:#6b7280;font-size:16px;line-height:1.6;max-width:600px;margin-bottom:44px;">{{ page.intro|richtext }}</div>{% endif %}

      {% if posts %}
      <div style="display:grid;grid-template-columns:repeat(2,1fr);gap:28px;">
        {% for post in posts %}
        <a href="{{ post.url }}" style="border:1px solid #f3f4f6;border-radius:14px;overflow:hidden;display:block;transition:box-shadow .15s;">
          {% if post.hero_image %}
            {% image post.hero_image fill-600x300 style="width:100%;height:180px;object-fit:cover;display:block;" %}
          {% else %}
            <div style="width:100%;height:180px;background:#f3f4f6;"></div>
          {% endif %}
          <div style="padding:24px;">
            <p style="font-size:12px;color:#9ca3af;margin-bottom:8px;">{{ post.published_date }} · {{ post.author }}</p>
            <h2 style="font-size:18px;font-weight:700;color:#111827;margin-bottom:8px;line-height:1.3;">{{ post.title }}</h2>
            {% if post.excerpt %}<p style="font-size:14px;color:#6b7280;line-height:1.6;">{{ post.excerpt }}</p>{% endif %}
          </div>
        </a>
        {% endfor %}
      </div>

      {% if posts.has_other_pages %}
      <div style="display:flex;gap:12px;justify-content:center;margin-top:44px;">
        {% if posts.has_previous %}<a href="?page={{ posts.previous_page_number }}" class="btn-outline" style="padding:9px 18px;font-size:14px;">← Previous</a>{% endif %}
        {% if posts.has_next %}<a href="?page={{ posts.next_page_number }}" class="btn-primary" style="padding:9px 18px;font-size:14px;">Next →</a>{% endif %}
      </div>
      {% endif %}

      {% else %}
      <p style="color:#9ca3af;font-size:15px;">No posts yet. Check back soon.</p>
      {% endif %}
    </div>
  </section>
  {% endblock %}
  ```

- [ ] **Step 4: Create `cms/templates/cms/blog_post_page.html`**
  ```html
  {% extends "cms/base.html" %}
  {% load wagtailcore_tags wagtailimages_tags %}

  {% block title %}{{ page.title }}{% endblock %}
  {% block meta_description %}{{ page.excerpt }}{% endblock %}

  {% block content %}
  <article style="padding: 64px 48px;">
    <div style="max-width: 720px; margin: 0 auto;">
      <a href="/blog/" style="color:#6366f1;font-size:13px;font-weight:600;display:inline-block;margin-bottom:28px;">← Back to Blog</a>
      <p style="font-size:13px;color:#9ca3af;margin-bottom:12px;">{{ page.published_date }} · {{ page.author }}</p>
      <h1 style="font-size:40px;font-weight:800;letter-spacing:-0.5px;line-height:1.2;margin-bottom:20px;color:#111827;">{{ page.title }}</h1>
      {% if page.excerpt %}<p style="font-size:18px;color:#6b7280;line-height:1.6;margin-bottom:36px;padding-bottom:36px;border-bottom:1px solid #f3f4f6;">{{ page.excerpt }}</p>{% endif %}
      {% if page.hero_image %}{% image page.hero_image width-720 style="width:100%;border-radius:12px;margin-bottom:36px;" %}{% endif %}
      <div style="font-size:16px;line-height:1.75;color:#374151;">
        {% for block in page.body %}
          {% if block.block_type == 'paragraph' %}{{ block.value|richtext }}
          {% elif block.block_type == 'heading' %}<h2 style="font-size:24px;font-weight:700;margin:36px 0 14px;color:#111827;">{{ block.value }}</h2>
          {% elif block.block_type == 'image' %}{% image block.value width-720 style="width:100%;border-radius:8px;margin:24px 0;" %}
          {% endif %}
        {% endfor %}
      </div>
    </div>
  </article>
  {% endblock %}
  ```

- [ ] **Step 5: Run tests**
  ```bash
  uv run python manage.py test tests.test_cms_pages.BlogURLTest -v 2
  ```
  Expected: Both tests pass.

- [ ] **Step 6: Commit**
  ```bash
  git add cms/templates/cms/blog_index_page.html cms/templates/cms/blog_post_page.html
  git commit -m "feat: add blog index and blog post templates"
  ```

---

### Task 11: Build About, Pricing, and Legal templates

**Files:**
- Create: `cms/templates/cms/about_page.html`
- Create: `cms/templates/cms/pricing_page.html`
- Create: `cms/templates/cms/legal_page.html`

- [ ] **Step 1: Add URL tests**
  Append to `tests/test_cms_pages.py`:
  ```python
  class MarketingPagesURLTest(DjangoTestCase):
      def test_about_returns_200(self):
          self.assertEqual(self.client.get('/about/').status_code, 200)

      def test_pricing_returns_200(self):
          self.assertEqual(self.client.get('/pricing/').status_code, 200)

      def test_privacy_returns_200(self):
          self.assertEqual(self.client.get('/privacy/').status_code, 200)

      def test_terms_returns_200(self):
          self.assertEqual(self.client.get('/terms/').status_code, 200)
  ```

- [ ] **Step 2: Run to confirm failures**
  ```bash
  uv run python manage.py test tests.test_cms_pages.MarketingPagesURLTest -v 2
  ```
  Expected: 4 failures — `TemplateDoesNotExist` for each.

- [ ] **Step 3: Create `cms/templates/cms/about_page.html`**
  ```html
  {% extends "cms/base.html" %}
  {% load wagtailcore_tags wagtailimages_tags %}
  {% block title %}About{% endblock %}
  {% block content %}
  <section style="padding: 72px 48px;">
    <div style="max-width: 720px; margin: 0 auto;">
      <div class="section-label">About</div>
      <h1 class="section-title">{{ page.title }}</h1>
      <div style="font-size:16px;line-height:1.75;color:#374151;">
        {% for block in page.body %}
          {% if block.block_type == 'paragraph' %}{{ block.value|richtext }}
          {% elif block.block_type == 'heading' %}<h2 style="font-size:24px;font-weight:700;margin:36px 0 14px;color:#111827;">{{ block.value }}</h2>
          {% elif block.block_type == 'image' %}{% image block.value width-720 style="width:100%;border-radius:8px;margin:24px 0;" %}
          {% endif %}
        {% endfor %}
        {% if not page.body %}<p style="color:#9ca3af;">About page content coming soon.</p>{% endif %}
      </div>
    </div>
  </section>
  {% endblock %}
  ```

- [ ] **Step 4: Create `cms/templates/cms/pricing_page.html`**
  ```html
  {% extends "cms/base.html" %}
  {% load wagtailcore_tags %}
  {% block title %}Pricing{% endblock %}
  {% block content %}
  <section style="padding: 72px 48px; text-align: center;">
    <div class="container" style="padding:0;">
      <div class="section-label">Pricing</div>
      <h1 class="section-title">{{ page.heading }}</h1>
      {% if page.subtext %}<p class="section-sub" style="margin:0 auto 44px;">{{ page.subtext }}</p>{% endif %}

      {% if page.tiers %}
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:20px;text-align:left;">
        {% for block in page.tiers %}
        {% with tier=block.value %}
        <div style="background:#fff;border:{% if tier.is_popular %}2px solid #6366f1{% else %}1px solid #e5e7eb{% endif %};border-radius:16px;padding:32px;position:relative;">
          {% if tier.is_popular %}<div style="position:absolute;top:-13px;left:50%;transform:translateX(-50%);background:#6366f1;color:#fff;font-size:11px;font-weight:700;padding:4px 14px;border-radius:20px;white-space:nowrap;">Most Popular</div>{% endif %}
          <div style="font-size:12px;font-weight:700;color:#9ca3af;text-transform:uppercase;letter-spacing:1px;margin-bottom:8px;">{{ tier.name }}</div>
          <div style="font-size:40px;font-weight:800;letter-spacing:-1px;margin-bottom:4px;">{{ tier.price }}<span style="font-size:15px;font-weight:500;color:#9ca3af;">{{ tier.period }}</span></div>
          <p style="color:#9ca3af;font-size:13px;margin-bottom:20px;">{{ tier.description }}</p>
          <a href="{{ tier.cta_url }}" style="display:block;text-align:center;padding:10px;border-radius:8px;font-size:14px;font-weight:600;margin-bottom:22px;{% if tier.is_popular %}background:#111827;color:#fff;{% else %}border:1px solid #e5e7eb;color:#374151;{% endif %}">{{ tier.cta_label }}</a>
          <ul style="list-style:none;display:flex;flex-direction:column;gap:10px;">
            {% for feature in tier.features %}<li style="font-size:13px;color:#6b7280;display:flex;gap:8px;"><span style="color:#6366f1;font-weight:700;">✓</span>{{ feature }}</li>{% endfor %}
          </ul>
        </div>
        {% endwith %}
        {% endfor %}
      </div>
      {% else %}
      <p style="color:#9ca3af;">Pricing details coming soon.</p>
      {% endif %}

      {% if page.faq %}
      <div style="max-width:640px;margin:72px auto 0;text-align:left;">
        <h2 style="font-size:26px;font-weight:800;margin-bottom:28px;">Frequently Asked Questions</h2>
        {% for block in page.faq %}
        <details style="border-bottom:1px solid #f3f4f6;padding:18px 0;">
          <summary style="font-size:15px;font-weight:600;cursor:pointer;color:#111827;">{{ block.value.question }}</summary>
          <div style="margin-top:12px;font-size:14px;color:#6b7280;line-height:1.7;">{{ block.value.answer|richtext }}</div>
        </details>
        {% endfor %}
      </div>
      {% endif %}
    </div>
  </section>
  {% endblock %}
  ```

- [ ] **Step 5: Create `cms/templates/cms/legal_page.html`**
  ```html
  {% extends "cms/base.html" %}
  {% load wagtailcore_tags %}
  {% block title %}{{ page.title }}{% endblock %}
  {% block content %}
  <section style="padding: 64px 48px;">
    <div style="max-width: 720px; margin: 0 auto;">
      <h1 style="font-size:34px;font-weight:800;letter-spacing:-0.5px;margin-bottom:32px;color:#111827;">{{ page.title }}</h1>
      <div style="font-size:15px;line-height:1.8;color:#374151;">{{ page.body|richtext }}</div>
    </div>
  </section>
  {% endblock %}
  ```

- [ ] **Step 6: Run all tests**
  ```bash
  uv run python manage.py test tests.test_cms_pages -v 2
  ```
  Expected: All tests pass.

- [ ] **Step 7: Commit**
  ```bash
  git add cms/templates/cms/about_page.html cms/templates/cms/pricing_page.html cms/templates/cms/legal_page.html tests/test_cms_pages.py
  git commit -m "feat: add about, pricing, and legal page templates"
  ```

---

## PHASE 4 — Dev Environment & CI (can run in parallel with Phase 3)

---

### Task 12: Add dev settings and passenger config

**Files:**
- Create: `vcard_project/settings_dev.py`
- Create: `passenger_wsgi_dev.py`

- [ ] **Step 1: Create `vcard_project/settings_dev.py`**
  ```python
  from .settings import *  # noqa: F401, F403

  DEBUG = True
  ALLOWED_HOSTS = ['devvcard.hosammo.com', 'localhost', '127.0.0.1']

  DATABASES = {
      'default': {
          'ENGINE': 'django.db.backends.sqlite3',
          'NAME': BASE_DIR / 'db_dev.sqlite3',
      }
  }

  STATIC_ROOT = BASE_DIR / 'static_collected'
  MEDIA_ROOT = BASE_DIR / 'media'
  WAGTAILADMIN_BASE_URL = 'https://devvcard.hosammo.com'
  ```

- [ ] **Step 2: Create `passenger_wsgi_dev.py`**
  ```python
  import os
  import sys

  sys.path.insert(0, os.path.dirname(__file__))
  os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'vcard_project.settings_dev')

  from django.core.wsgi import get_wsgi_application  # noqa: E402
  application = get_wsgi_application()
  ```

- [ ] **Step 3: Verify dev settings load correctly**
  ```bash
  uv run python manage.py check --settings=vcard_project.settings_dev
  ```
  Expected: `System check identified no issues (0 silenced).`

- [ ] **Step 4: Commit**
  ```bash
  git add vcard_project/settings_dev.py passenger_wsgi_dev.py
  git commit -m "feat: add dev environment settings and Passenger config for devvcard.hosammo.com"
  ```

---

### Task 13: Add GitHub Actions auto-deploy workflow

**Files:**
- Create: `.github/workflows/deploy-dev.yml`

- [ ] **Step 1: Create directory**
  ```bash
  mkdir -p .github/workflows
  ```

- [ ] **Step 2: Create `.github/workflows/deploy-dev.yml`**
  ```yaml
  name: Deploy to devvcard.hosammo.com

  on:
    push:
      branches: [dev]

  jobs:
    deploy:
      runs-on: ubuntu-latest

      steps:
        - name: Deploy via SSH
          uses: appleboy/ssh-action@v1
          with:
            host: ${{ secrets.DEV_SSH_HOST }}
            username: ${{ secrets.DEV_SSH_USER }}
            key: ${{ secrets.DEV_SSH_KEY }}
            port: ${{ secrets.DEV_SSH_PORT }}
            script: |
              set -e
              cd ~/devvcard

              echo "▶ Pulling latest dev branch..."
              git pull origin dev

              echo "▶ Installing dependencies..."
              uv sync --frozen

              echo "▶ Running migrations..."
              uv run python manage.py migrate --settings=vcard_project.settings_dev

              echo "▶ Collecting static files..."
              uv run python manage.py collectstatic --noinput --settings=vcard_project.settings_dev

              echo "▶ Restarting Passenger..."
              mkdir -p tmp && touch tmp/restart.txt

              echo "✅ Deploy complete → https://devvcard.hosammo.com"
  ```

- [ ] **Step 3: Commit**
  ```bash
  git add .github/
  git commit -m "ci: add GitHub Actions auto-deploy to devvcard.hosammo.com on push to dev"
  ```

---

## PHASE 5 — Final verification

---

### Task 14: Full smoke test and push

**Files:** none (verification only)

- [ ] **Step 1: Run full test suite**
  ```bash
  uv run python manage.py test tests/ -v 2
  ```
  Expected: All tests pass. Note the count — should be at least 10 tests.

- [ ] **Step 2: Start server and manually verify all routes**
  ```bash
  uv run python manage.py runserver
  ```
  Check each URL manually:
  - `http://127.0.0.1:8000/` — homepage renders with nav, hero, pricing, footer
  - `http://127.0.0.1:8000/blog/` — blog index renders
  - `http://127.0.0.1:8000/about/` — about page renders
  - `http://127.0.0.1:8000/pricing/` — pricing page renders
  - `http://127.0.0.1:8000/privacy/` — privacy page renders
  - `http://127.0.0.1:8000/terms/` — terms page renders
  - `http://127.0.0.1:8000/cms/` — Wagtail admin loads
  - `http://127.0.0.1:8000/dashboard/` — SaaS dashboard still works (redirects to login if not authenticated)
  - `http://127.0.0.1:8000/login/` — login page still works

- [ ] **Step 3: Add CMS content via Wagtail admin**
  Log in at `http://127.0.0.1:8000/cms/` with your superuser account.
  - Navigate to Pages → Home → Edit
  - Add 6 features, 3 steps, 3 testimonials
  - Publish the page
  - Reload `http://127.0.0.1:8000/` — confirm sections appear

- [ ] **Step 4: Push dev branch to GitHub**
  ```bash
  git push origin dev
  ```
  Expected: Push succeeds. If GitHub Actions secrets are configured, the deploy workflow will trigger automatically.

- [ ] **Step 5: Verify GitHub Actions workflow ran (if secrets are set)**
  Go to `https://github.com/hosammo/vcard/actions` — confirm the `Deploy to devvcard.hosammo.com` workflow ran and shows green.

---

## Server Setup Checklist (manual, done by developer on hosting)

These steps are done once via SSH on the server — not automated.

- [ ] Create subdomain `devvcard.hosammo.com` in cPanel → Points to `~/devvcard/public_html/` or `~/devvcard/`
- [ ] SSH into server: `ssh user@hosammo.com`
- [ ] Install uv on server: `curl -LsSf https://astral.sh/uv/install.sh | sh`
- [ ] Clone repo: `git clone https://github.com/hosammo/vcard.git ~/devvcard && cd ~/devvcard && git checkout dev`
- [ ] Create `~/devvcard/.env` with `SECRET_KEY=<strong-random-key>` and `DEBUG=True`
- [ ] Copy `passenger_wsgi_dev.py` to `passenger_wsgi.py` in the app root (Passenger looks for this filename)
- [ ] Run initial setup:
  ```bash
  cd ~/devvcard
  uv sync
  uv run python manage.py migrate --settings=vcard_project.settings_dev
  uv run python manage.py collectstatic --noinput --settings=vcard_project.settings_dev
  uv run python manage.py createsuperuser --settings=vcard_project.settings_dev
  ```
- [ ] Generate a deploy SSH keypair: `ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/deploy_key`
- [ ] Add public key to server: `cat ~/.ssh/deploy_key.pub >> ~/.ssh/authorized_keys`
- [ ] Add these secrets to GitHub repo (Settings → Secrets → Actions):
  - `DEV_SSH_HOST` — server hostname
  - `DEV_SSH_USER` — your cPanel username
  - `DEV_SSH_KEY` — contents of `~/.ssh/deploy_key` (private key)
  - `DEV_SSH_PORT` — `22`
